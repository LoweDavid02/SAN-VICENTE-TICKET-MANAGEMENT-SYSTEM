# Redirects for SPA routing on Render/Netlify/Vercel
# This ensures all routes fallback to index.html for client-side routing

# API requests should not be redirected
/api/* https://your-backend-url.onrender.com/api/:splat 200

# All other routes fallback to index.html (SPA)
/*    /index.html   200
